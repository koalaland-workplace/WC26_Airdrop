export interface HomeNationRow {
  flag: string;
  name: string;
  points: string;
  delta: string;
  isNegative?: boolean;
}

export interface HomeHeroSnapshot {
  warLive: string;
  warDelta: string;
  liveFans: number;
}
